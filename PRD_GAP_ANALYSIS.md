# PRD 缺口分析

> PRD：260702-咨询诊断Agent_PRD_V1.docx  
> 对照时间：2026-07-07  
> 对照范围：当前 `E:\Consultation_agent` 全部代码

---

## 一、P0 功能（必须有）— 全部完成

| # | PRD 要求 | 当前状态 | 对应文件 |
|---|---|---|---|
| P0-1 | 自测落地页 + 二维码入口 | ✅ 完成 | 前端 intro 页 + 渠道二维码 API |
| P0-2 | 企业信息收集（公司/行业/规模/姓名/职位/联系方式） | ✅ 完成 | `POST /api/public/leads` |
| P0-3 | 68 题 10 模块问卷（0-4 分） | ✅ 完成 | 正式题库 JSON + `GET /api/public/questions` |
| P0-4 | 评分引擎 + 风险分层（260 分制） | ✅ 完成 | `service/scoring.py` |
| P0-5 | AI 诊断报告生成（管理摘要 + 短板 + 建议） | ✅ 完成 | `service/reporting.py`，含 fallback 兜底 |
| P0-6 | 案例推荐（按低分维度 + 行业匹配） | ✅ 完成 | `select_recommendations()` |

---

## 二、P1 功能（应该有）

| # | PRD 要求 | 当前状态 | 差距说明 |
|---|---|---|---|
| P1-1 | 线索后台列表 + 筛选 + 导出 | ✅ 完成 | admin.py leads + CSV export |
| P1-2 | PDF 下载 | ✅ 完成 | ReportLab PDF + 柱状图 + 雷达图 |
| P1-3 | **Word 下载** | ❌ 未实现 | PRD 提了"PDF/Word"，当前只有 PDF |

---

## 三、PRD 细节要求 — 实际遗漏项

### 缺口 1：联系方式门槛未生效（P0 影响）

**PRD 原文**（4.2）："联系方式为必填；未授权联系时可看摘要但不提供完整报告"  
**PRD 原文**（4.5）："用户查看报告摘要，填写/确认联系方式后获取完整报告"

**当前实现**：提交问卷后直接展示全部报告内容，包括 HTML 正文。没有"摘要页 → 填联系方式 → 完整报告"的流程。

**影响**：无法过滤"只看看不填联系方式的客户"，损失获客线索。

**建议修改**：
- 公开报告页（`/report/:token`）首次访问只展示分数卡片 + 图表，报告正文折叠或模糊
- 未填联系方式时弹出表单，填写后解锁完整报告
- 或者提交问卷时强制要求联系方式（当前已要求 phone/wechat 至少一个，但不够严格）

---

### 缺口 2：案例库数量不足 30 条（验收标准）

**PRD 验收标准**："至少沉淀 30 条可维护案例"

**当前实现**：`seed.py` 只有 9 条硬编码案例。后台可以手动添加，但首次部署只有 9 条。

**影响**：低分维度可能匹配不到案例，推荐内容单一。

**建议修改**：
- 整理 30 条案例，补充到 seed.py 或单独 JSON 文件
- 覆盖所有 10 个模块和主要行业

---

### 缺口 3：报告尾页无微信/顾问引导（P1 影响）

**PRD 原文**（8.2）："报告尾页引导'如需结合业务场景深度解读，可添加顾问微信/预约诊断'"

**当前实现**：`render_fallback_html()` 中的"下一步建议"是通用文案："建议安排一次 60-90 分钟顾问诊断会"，没有可配置的微信二维码或联系方式。

**影响**：缺少咨询转化入口。

**建议修改**：
- 报告末尾增加可配置的联系引导区（顾问微信二维码 + 预约链接）
- 配置项放在 `.env` 或后台设置中

---

### 缺口 4：埋点事件覆盖不全（P1 影响）

**PRD 七**：列举了 13 个埋点事件

| PRD 要求事件 | 当前是否埋点 |
|---|---|
| 进入官网 | ✅ `enter_site` |
| 点击开始自测 | ✅ `click_start` |
| 提交客户信息 | ✅ `submit_customer_info` |
| 开始问卷 | ✅ `start_questionnaire` |
| **完成模块** | ❌ 未埋点 |
| 提交问卷 | ✅ `submit_questionnaire` |
| **生成报告成功** | ❌ 缺少 `generate_report_success`（只在 App.vue 前端调了 track） |
| **生成报告失败** | ❌ 缺少 |
| 查看报告摘要 | ✅ `view_report_summary` |
| **领取完整报告** | ❌ 缺少（`claim_full_report` 在 PDF 下载时触发，语义不完全一致） |
| **点击添加微信/咨询** | ❌ 未埋点 |
| **后台查看线索** | ❌ 未埋点 |
| **导出线索** | ❌ 未埋点（但有 ExportLog 记录） |

**建议修改**：补齐缺失的 6 个埋点事件。

---

### 缺口 5：移动端微信浏览器未专门适配（P1 影响）

**PRD 原文**（6.2）："移动端微信内置浏览器优先"

**当前实现**：CSS 有 `@media (max-width: 820px)` 响应式，做了 flex 堆叠和字号缩放。但未测试微信内置浏览器的兼容性（如 `backdrop-filter` 在微信浏览器中不支持、100vh 在微信中有地址栏问题等）。

**建议修改**：
- 将 `backdrop-filter`、`-webkit-backdrop-filter` 加 fallback
- 避免 `100vh`，改用 `100dvh` 或 `min-height: 100%`
- 微信浏览器中测试一遍完整流程

---

### 缺口 6：问卷"上一页/下一页"分页被改为全量展示（UX 分歧）

**PRD 原文**（4.3）："按模块展示题目，支持上一页/下一页，展示完成进度"

**当前实现**：全部 68 题纵向排列，一页到底，用模块标题做分区。

**分析**：这个改动是之前用户要求的——"问题从上到下排序展示"。与 PRD 的分页模式存在分歧。两种方案各有利弊：
- 分页：减少认知负荷，进度感更强，完成率可能更高
- 全量：方便快速浏览和跳答，但 68 题一页太长

**建议**：保持当前全量滚动 + 底部固定提交按钮，但在长列表中增加"回到顶部"和模块快速导航。

---

### 缺口 7：后台未展示报告生成异常（P1 影响）

**PRD 原文**（4.5）："后台记录异常"

**当前实现**：`Report.generation_error` 字段有存错误信息，但后台查看报告时未展示此字段。管理员看不到哪些报告是 fallback 生成的。

**建议修改**：在 `GET /api/admin/reports/{id}` 返回中增加 `generation_error` 字段，后台列表页增加报告状态筛选。

---

### 缺口 8：数据库选型未达 PRD 推荐（低优先级）

**PRD 原文**（11.1）："PostgreSQL / MySQL / Supabase 类托管数据库"

**当前实现**：开发环境 SQLite，生产环境 MySQL。没有 PostgreSQL 支持。

**分析**：PRD 说 MySQL 也可接受，当前 SQLite→MySQL 的切换路径已就绪。非硬性缺口。

---

### 缺口 9：Word 导出未实现（P2 增强）

**PRD 原文**（11.1）："MVP 先用网页报告；增强版再导出 PDF/Word"

**当前实现**：PDF 有，Word 没有。

**建议**：P2 阶段用 `python-docx` 实现。

---

## 四、总结表格

| 级别 | 数量 | 内容 |
|---|---|---|
| ✅ 已完成 | 8 项 | P0 全量 + P1 的线索后台、PDF |
| ⚠️ 功能缺失 | 2 项 | Word 导出、案例库 30 条 |
| ⚠️ 逻辑缺位 | 3 项 | 联系方式门槛、报告尾页微信引导、埋点不全 |
| ⚠️ 适配不足 | 2 项 | 微信浏览器适配、后台异常展示 |
| 📋 PRD 明确后置 | 3 项 | 顾问复核台、行业案例 RAG、领域小模型 |

---

## 五、建议修改优先级

| 排序 | 缺口 | 工作量 | 影响 |
|---|---|---|---|
| **第 1** | 案例库补到 30 条 | 1 小时（整理数据） | 推荐质量、验收标准 |
| **第 2** | 报告尾页增加微信/顾问引导 | 30 分钟（改 HTML 模板） | 咨询转化 |
| **第 3** | 补齐埋点事件 | 1 小时（6 个事件） | 数据分析 |
| **第 4** | 后台展示报告异常状态 | 15 分钟（加字段） | 运维监控 |
| **第 5** | 联系方式门槛逻辑 | 1 小时 | 获客线索 |
| **第 6** | 微信浏览器适配 | 2 小时 | 移动端体验 |
| **第 7** | Word 导出 | 3 小时 | PRD P1 要求 |
